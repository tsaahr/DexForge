<!DOCTYPE html>
<html>
  <head>
    <title>Pokedex</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>


    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  </head>

  <body class="bg-light">
    <header class="bg-danger text-white shadow-sm py-3 mb-4">
      <div class="container d-flex justify-content-between align-items-center">
        <h1 class="mb-0" style="font-size: 1.8rem;">
          <%= link_to "Pokédex", pokedex_index_path, class: "text-white text-decoration-none" %>
        </h1>
        <nav>
          <%= link_to "Home", pokedex_index_path, class: "btn btn-outline-light me-2" %>
        </nav>
      </div>
    </header>

    <main class="container mb-5">
      <%= yield %>
    </main>

    <footer class="text-center text-muted py-3 border-top">
      <small>Pokédex App © <%= Time.current.year %></small>
    </footer>
  </body>
</html>
