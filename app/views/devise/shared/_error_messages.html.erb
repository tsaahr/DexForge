<% if resource.errors.any? %>
  <div class="alert alert-danger">
    <h5 class="mb-3">There were some problems with your submission:</h5>
    <ul class="mb-0">
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
